# Ayven outputs

Copy a `validation/runs/<timestamp>` directory here when you want a human to read it beside a reference answer. The GPU harness already archives the run under `/workspace/ayven-results/` or `$HOME/ayven-results`.

Do not treat a DRY-RUN folder as model quality.
