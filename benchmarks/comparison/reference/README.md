# Reference answers

Paste a frontier assistant's answer for the same frozen exam here, one file per benchmark. This repository does not call that API.

Keep the paste unmodified. Score it with `../scores/RUBRIC.md` without looking at which system wrote which file if you can.
